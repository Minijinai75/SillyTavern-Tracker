import { setNestedValue } from "../../utils/ObjectUtils.js";

export function buildEditor(tracker, fieldDefs) {
	// TODO: return DOM fragment with controls for each field
	// On input change -> tracker.set(fieldPath, newValue)
	return document.createElement("div");
}
