import TrackerStore from "../../store/TrackerStore.js";

export function injectPresetSelector(entityId, mountEl) {
	// TODO: build <select> with presets, onChange -> TrackerStore.setEntityPreset(entityId, newPresetId)
	// Persist via Settings UI save routine.
}
