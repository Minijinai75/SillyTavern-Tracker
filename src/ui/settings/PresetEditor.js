import { openPresetFieldSelector } from "./PresetFieldSelector.js";

export function openPresetEditor(presetId) {
	// TODO: load preset data, edit templates/overrides, open field selection UI
	// button: "Select Fields" -> openPresetFieldSelector(presetId)
}
